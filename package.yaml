packages:
  device: !include device/device.yaml
  sensors: !include device/sensors.yaml
  lvgl: !include device/lvgl.yaml
  time: !include addon/time.yaml
  backlight: !include addon/backlight.yaml
  network: !include addon/network.yaml
  fonts: !include assets/fonts.yaml
  icons: !include assets/icons.yaml
  global-theme: !include theme/global.yaml
  button-theme: !include theme/button.yaml
  container-theme: !include theme/container.yaml
  chart: !include addon/solar-chart/solar-chart.yaml
  weather: !include addon/weather.yaml
  people: !include addon/people.yaml
  car: !include addon/car.yaml
  #heating: !include addon/heating.yaml

# Temporary workaround for the missing lv_conf.h file, which is required for the LVGL component. This will be removed once the PR is merged and the component is available in the registry.
external_components:
  - source: github://pr#16362
    components: [lvgl]
    refresh: 1h