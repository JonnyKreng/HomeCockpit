# ESP32-P4-JC1060P470
substitutions:
  name: "esp-display"
  friendly_name: "ESP Display"

# Wifi Setup
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  device: !include package.yaml